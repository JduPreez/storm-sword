## Deployment

1. `./build.sh`
2. `sst dev` is running for the first time or 
`sst deploy` if SST is already running.

## Shared Package Docs

- Core functional helper docs (including `partial!` and `partial_right!`): [services/packages/core/README.md](services/packages/core/README.md)